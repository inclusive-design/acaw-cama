    <meta name="description" content="{{ site[lang].description }}">
    <meta property="og:title" content="{% if title %}{{ title }} | {% endif %}{{ site[lang].name }}">
    <meta property="og:description" content="{{ site[lang].description }}">
    <meta property="og:locale" content="{{ lang }}">
    <meta property="og:type" content="website">
    <meta property="og:url" content="{{ site[lang].url }}{{ page.url }}">
    <link rel="canonical" href="{{ site[lang].url }}{{ page.url }}">
    {% set links = page.url | locale_links %}
    {%- for link in links %}
    <link rel="alternate" hreflang="{{ link.lang }}" href="{{ site[lang].url }}{{ link.url }}">
    {%- endfor -%}
